{
  "manifest_version": 2,
  "name": "AppWorkspace",
  "description": "Chrome app to allow opening of tabs in standalone windows (as if the page were it's own app)",
  "version": "2.0",

  "icons": {
    "16": "icon.png"
  },

  "app": {
    "background": {
      "scripts": [
        "appWorkspace.js",
        "background.js"
      ]
    }
  },

  "permissions": [
    "contextMenus",
    "webview"
  ]
}