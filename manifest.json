{
  "manifest_version": 2,
  "name": "AppTab",
  "description": "Chrome extension to allow opening of tabs in standalone windows (as if the page were it's own app)",
  "version": "1.1",

  "icons": {
    "16": "icon.png"
  },

  "permissions": [
   "contextMenus",
   "webNavigation"
   ],

  "background": {
    "scripts": ["newAppTab.js"]
  }
}